{% extends 'base.html.twig' %}

{% block title %}Edit Task{% endblock %}

{% block body %}
    <h1>MODIFIER VOTRE TACHE</h1>

    {{ include('task/_form.html.twig', {'button_label': 'ENREGISTRER LES MODIFICATIONS'}) }}

    <div id="editTaches">
        <a href="{{ path('app_task_index') }}">RETOUR A LA LISTE</a>

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div id="deleteTaches">{{ include('task/_delete_form.html.twig') }}</div>
        {% else %}
            {# Redirection vers la page de connexion, par exemple #}
            {% set redirectionUrl = path('app_login') %}
        {% endif %}    </div>
{% endblock %}
